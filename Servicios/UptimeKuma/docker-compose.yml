version: '3.3'

services:
uptime-kuma:
image: louislam/uptime-kuma:1
container_name: uptime-kuma
restart: unless-stopped
ports:
- 3001:3001
volumes:
- /opt/uptime-kuma/data:/app/data # Asegurate de que esta ruta coincida con tu host
environment:
- TZ=America/Argentina/Buenos_Aires
